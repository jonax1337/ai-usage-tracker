{
  "name": "aiusage-tracker",
  "version": "1.7.0",
  "description": "Local zero-dependency dashboard and CLI for multi-provider AI usage, costs, plan limits, and pace predictions (Claude Code, Codex, OpenRouter, Z.ai/GLM)",
  "type": "module",
  "bin": {
    "aiusage": "dist/cli.js"
  },
  "files": [
    "dist",
    "public",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "scripts": {
    "start": "node server.ts",
    "cli": "node cli.ts",
    "build": "tsc -p tsconfig.web.json",
    "build:node": "tsc -p tsconfig.node.json",
    "check": "tsc -p tsconfig.json && tsc -p tsconfig.web.json --noEmit",
    "prepublishOnly": "npm run check && npm run build && npm run build:node"
  },
  "keywords": [
    "ai",
    "usage",
    "claude",
    "codex",
    "openrouter",
    "zai",
    "glm",
    "tokens",
    "dashboard",
    "cli",
    "rate-limits",
    "local-first",
    "typescript"
  ],
  "author": "Jonas Laux",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jonax1337/ai-usage-tracker.git"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
